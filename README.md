# react-table
Options: TableItem:

```ts
  headers: string[]
  rows: object[] 
  excludes?: object[] (optional) //disable items on search filter
  selectable?: boolean;
  button?: string;
  refresh?: boolean;
  onButtonTrigger?: () => void;
  onRefresh?: () => void;
```
